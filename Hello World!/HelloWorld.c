#include <stdio.h>  //Librería necesaria que contiene como ejecutar la función printf.

int main () //indica el inicio o punto de partida del código, se ejecutará primero lo que esta entre las llaves.
{
  //función que inprime en la consola lo que esta entre paréntesis. 
  
  printf("Hello World\n");
}
